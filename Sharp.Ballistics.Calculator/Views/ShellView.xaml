<Window x:Class="Sharp.Ballistics.Calculator.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:cal="http://www.caliburnproject.org"
        Width="800" Height="600"
         MinWidth="800" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        x:Name="MainWindow">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ItemsControl ItemsSource="{Binding FunctionScreens}" x:Name="Functions" Grid.Row="0" Margin="5 0 0 0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="65" Height="65" Margin="5" cal:Message.Attach="[Event Click] = [Action ActivateItem($dataContext)]">
                        <Image Source="{Binding Path=Icon}" Width="55" Height="55"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ContentControl Grid.Row="1" Margin="5" x:Name="ActiveItem" />
    </Grid>

</Window>